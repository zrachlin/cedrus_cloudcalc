/**
 * `components/index.js` exists simply as a 'central export' for components.
 * This way, we can import all of our components from the same place, rather than
 * having to figure out which file they belong to!
 */
export { default as Navbar } from './Navbar';
export { default as NotFound } from './NotFound';
export { default as StudentFormDialog } from './StudentFormDialog';
export { default as StudentsTable } from './StudentsTable';
export { default as CampusFormDialog } from './CampusFormDialog';
export { default as styles } from './styles';
